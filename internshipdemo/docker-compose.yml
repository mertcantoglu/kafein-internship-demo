version: '3.8'
services:
  mysql-db:
    image: mysql:8.0
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: mydatabase
      MYSQL_USER: user
      MYSQL_PASSWORD: userpassword
    ports:
      - ":3307"
    volumes:
      - mysql_data:/var/lib/mysql

  spring-boot-app:
    image: myapp
    container_name: spring-boot-app
    depends_on:
      - mysql-db
    ports:
      - ":8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql-db:3307/mydatabase
      SPRING_DATASOURCE_USERNAME: user
      SPRING_DATASOURCE_PASSWORD: userpassword
    networks:
      - mynetwork

volumes:
  mysql_data:

networks:
  mynetwork:
    driver: bridge
